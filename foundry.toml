[profile.default]
src = 'src'
out = 'out'
libs = ['lib']

[rpc_endpoints]
polygon = "${POLYGON_RPC_URL}"

[etherscan]
polygon = { key = "POLYSCAN_API_KEY" }

# For local deployment, use:
# forge script script/Profile.s.sol:ProfileScript --fork-url http://localhost:8545 --broadcast

# For testnet deployment, use:
# forge script script/Profile.s.sol:ProfileScript --rpc-url $MATIC_RPC_URL --broadcast --verify -vvvv


# See more config options https://github.com/foundry-rs/foundry/tree/master/config